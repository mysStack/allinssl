# GHCR 镜像标签。生产环境建议改为已验证的 sha-<commit> 标签。
ALLINSSL_IMAGE=ghcr.io/mysstack/allinssl:custom

# 对外暴露的主机端口。
ALLINSSL_PORT=8888

# 以下值仅会在空数据卷第一次启动时写入，必须修改为自己的值。
ALLINSSL_USER=allinssl
ALLINSSL_PWD=change-this-password
ALLINSSL_URL=/change-this-to-a-random-path
