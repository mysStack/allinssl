apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: allinssl
resources:
  - namespace.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
secretGenerator:
  - name: allinssl-env
    envs:
      - secret.env
generatorOptions:
  disableNameSuffixHash: true
